{
  "name": "@eticketsgo/worker",
  "version": "0.1.0",
  "private": true,
  "description": "ETicketsGo background worker — scheduled hold/booking expiry.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "ts-node --transpile-only src/main.ts",
    "start": "node dist/main.js",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@eticketsgo/api": "*",
    "@nestjs/common": "^10.4.4",
    "@nestjs/core": "^10.4.4",
    "@sentry/node": "^8.55.2",
    "bullmq": "^5.13.2",
    "ioredis": "^5.4.1",
    "prom-client": "^15.1.3",
    "reflect-metadata": "^0.2.2"
  },
  "devDependencies": {
    "@types/node": "^20.16.10",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
